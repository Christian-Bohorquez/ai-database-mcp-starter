{
  "_note": "Future/verify example for Claude Desktop MCP setup. Confirm client version supports HTTP MCP server URLs.",
  "mcpServers": {
    "dbhub": {
      "url": "http://localhost:5678/mcp",
      "_requires": "Start scripts/start-dbhub-http.ps1 first"
    }
  }
}